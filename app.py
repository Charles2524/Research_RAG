"""Streamlit single-page UI (SPEC 8, Phase 8).

Discovery/fetch is a separate, explicitly user-triggered action; chat queries
never touch the network (C2). Run with ``streamlit run app.py``.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("Phase 8")


if __name__ == "__main__":
    main()
