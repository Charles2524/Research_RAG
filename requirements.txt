# Pinned at each phase gate.
# Phase 0: config + tests
PyYAML==6.0.3
python-dotenv==1.2.3
pytest==9.1.1
# Phase 1: the only network client; used solely in sources/ and fetch.py (C1)
requests==2.34.2
# Phase 2: PDF -> markdown (needs the MS Visual C++ 2015-2022 x64 runtime on Windows)
pymupdf==1.28.2
pymupdf4llm==1.28.2
# Phase 2: embedding-model tokenizer for token-measured chunks; hub client used only in fetch.py
tokenizers==0.23.2
huggingface_hub==1.31.0
# Phase 3: embeddings (CPU-only torch; pip on Windows resolves the CPU wheel) and vector index
numpy==2.5.3
torch==2.14.0
sentence-transformers==6.0.1
transformers==5.17.0
sqlite-vec==0.1.9
# Phase 8: UI
streamlit==1.63.0
